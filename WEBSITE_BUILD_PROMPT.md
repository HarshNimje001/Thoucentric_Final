# Master website build prompt

Use this folder as the source of truth. Preserve the supplied case/report logic, keep Case Fact vs Inference vs Assumption vs Recommendation labels visible, render the supplied Draw.io files without inventing content, attach the three team photos only in the `18 / THE TEAM` section, and follow the section order in `index.html`. Do not add external statistics or unsupported financial claims. The website is a static, polished strategy workspace for EIC Option C.
